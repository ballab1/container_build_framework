#!/bin/bash

# copy definition files to environment folder
for file in $( lib.getFiles "${CBF['action']}/02.users_groups" ) ; do
    lib.copyFileToRuntimeEnvironment "$file"
done

for file in $( lib.getFiles "${CBF['action']}/03.downloads" ) ; do
    lib.copyFileToRuntimeEnvironment "$file"
done