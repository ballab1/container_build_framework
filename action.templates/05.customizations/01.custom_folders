#!/bin/bash

declare -r dirs='bin etc home lib lib64 media mnt opt root sbin usr var www'

for dir in ${dirs} ; do
    [ -d "${CBF['base']}/${dir}" ] && cp -r "${CBF['base']}/${dir}/"* "/${dir}/"
done

true
